// NOT USED. The project moved away from Supabase/any database -- the app now
// reads bundled local JSON files instead (see lib/dealscope-data.ts, which
// imports data/companies.json and data/deals.json directly). This file is
// kept only so old references don't 404; safe to delete.
export {}
