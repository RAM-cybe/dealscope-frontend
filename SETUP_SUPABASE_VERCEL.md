# NOT USED — see SETUP.md instead

This project no longer uses Supabase or any database. The data is bundled
directly into the app as local JSON files (`data/companies.json`,
`data/deals.json`), generated by `export_for_frontend.py` at the repo root.

See `SETUP.md` in this folder for the current steps: run locally, then push
and host on Vercel — no accounts beyond GitHub + Vercel required.
