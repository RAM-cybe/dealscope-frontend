# NOT NEEDED. This project reads data from bundled local JSON files
# (data/companies.json, data/deals.json) instead of a database, so no
# environment variables or API keys are required to run or deploy it.
